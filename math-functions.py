import math

print(math.ceil(3.7)) # rounds up to the nearest integer
print(math.floor(2.3)) # rounds down to the nearest integer
x = 2.9
print(round(x)) # rounds to the nearest integer
print(abs(-2.9)) # returns the absolute value always positive